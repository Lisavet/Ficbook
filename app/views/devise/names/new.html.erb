<h2>Forgot your name?</h2>

<%= form_for(resource, as: resource_name, url: name_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="actions">
    <%= f.submit "Send me reset name instructions" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>