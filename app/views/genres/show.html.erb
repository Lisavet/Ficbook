<p id="notice"><%= notice %></p>

<h1><%= @genre.name %></h1>
<!--<p>-->
<!--  <strong>Name:</strong>-->
  <%#= @genre.name %>
<!--</p>-->

<section id="fanfics">
  <%= render @fanfics %>
</section>

<%= will_paginate @fanfics, renderer: 'BootstrapPaginationHelper::LinkRenderer' %>

<span class="btn btn-outline-primary"> <%= link_to 'Edit', edit_genre_path(@genre) %> </span>
<span class="btn btn-outline-primary"> <%= link_to 'Back', genre_path %> </span>
